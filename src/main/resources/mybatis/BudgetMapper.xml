<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.mapper.BudgetMapper">

    <resultMap id="BaseResultMap" type="com.finance.po.Budget">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="category_id" jdbcType="BIGINT" property="categoryId"/>
        <result column="cycle_type" jdbcType="TINYINT" property="cycleType"/>
        <result column="cycle_value" jdbcType="VARCHAR" property="cycleValue"/>
        <result column="budget_amount" jdbcType="DECIMAL" property="budgetAmount"/>
        <result column="used_amount" jdbcType="DECIMAL" property="usedAmount"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, category_id, cycle_type, cycle_value, budget_amount, used_amount, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.finance.po.Budget" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `budget` (user_id, category_id, cycle_type, cycle_value, budget_amount, used_amount, create_time, update_time)
        VALUES (#{userId}, #{categoryId}, #{cycleType}, #{cycleValue}, #{budgetAmount}, #{usedAmount}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `budget`
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="selectByUniqueKey" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `budget`
        WHERE user_id = #{userId}
        AND category_id = #{categoryId}
        AND cycle_type = #{cycleType}
        AND cycle_value = #{cycleValue}
    </select>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `budget`
        WHERE user_id = #{userId}
        ORDER BY cycle_type ASC, cycle_value DESC, category_id ASC
    </select>

    <select id="selectByUserIdAndCycle" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `budget`
        WHERE user_id = #{userId}
        AND cycle_type = #{cycleType}
        AND cycle_value = #{cycleValue}
        ORDER BY category_id ASC
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.finance.po.Budget">
        UPDATE `budget`
        <set>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="cycleType != null">
                cycle_type = #{cycleType},
            </if>
            <if test="cycleValue != null">
                cycle_value = #{cycleValue},
            </if>
            <if test="budgetAmount != null">
                budget_amount = #{budgetAmount},
            </if>
            <if test="usedAmount != null">
                used_amount = #{usedAmount},
            </if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="updateUsedAmount">
        UPDATE `budget`
        SET used_amount = used_amount + #{usedAmountDelta},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteByPrimaryKey">
        DELETE FROM `budget`
        WHERE id = #{id} AND user_id = #{userId}
    </delete>
</mapper>