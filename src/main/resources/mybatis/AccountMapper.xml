<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.mapper.AccountMapper">

    <resultMap id="BaseResultMap" type="com.finance.po.Account">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="account_name" jdbcType="VARCHAR" property="accountName"/>
        <result column="account_type" jdbcType="VARCHAR" property="accountType"/>
        <result column="initial_balance" jdbcType="DECIMAL" property="initialBalance"/>
        <result column="current_balance" jdbcType="DECIMAL" property="currentBalance"/>
        <result column="is_enabled" jdbcType="TINYINT" property="isEnabled"/>
        <result column="remark" jdbcType="VARCHAR" property="remark"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, account_name, account_type, initial_balance, current_balance, is_enabled, remark, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.finance.po.Account" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `account` (user_id, account_name, account_type, initial_balance, current_balance, is_enabled, remark, create_time, update_time)
        VALUES (#{userId}, #{accountName}, #{accountType}, #{initialBalance}, #{currentBalance}, #{isEnabled}, #{remark}, #{createTime}, #{updateTime})
    </insert>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `account`
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `account`
        WHERE user_id = #{userId}
        ORDER BY create_time ASC
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.finance.po.Account">
        UPDATE `account`
        <set>
            <if test="accountName != null">
                account_name = #{accountName},
            </if>
            <if test="accountType != null">
                account_type = #{accountType},
            </if>
            <if test="initialBalance != null">
                initial_balance = #{initialBalance},
            </if>
            <if test="currentBalance != null">
                current_balance = #{currentBalance},
            </if>
            <if test="isEnabled != null">
                is_enabled = #{isEnabled},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="updateAccountBalance">
        UPDATE `account`
        SET current_balance = current_balance + #{amount},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteByPrimaryKey">
        DELETE FROM `account`
        WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <select id="sumCurrentBalanceByUserId" parameterType="java.lang.Long" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(current_balance), 0.00)
        FROM `account`
        WHERE user_id = #{userId} AND is_enabled = 1
    </select>
</mapper>