<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.mapper.AccountMapper">

    <select id="selectByUserId" resultType="com.finance.po.Account">
        SELECT * FROM account WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <insert id="insert">
        INSERT INTO account (user_id, account_name, account_type, initial_balance, current_balance, remark, create_time, update_time)
        VALUES (#{userId}, #{accountName}, #{accountType}, #{initialBalance}, #{currentBalance}, #{remark}, NOW(), NOW())
    </insert>

    <select id="selectById" resultType="com.finance.po.Account">
        SELECT * FROM account WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE account
        <set>
            <if test="accountName != null">account_name = #{accountName},</if>
            <if test="accountType != null">account_type = #{accountType},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM account WHERE id = #{id}
    </delete>

</mapper>